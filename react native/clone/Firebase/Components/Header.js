import { View, Text,SafeAreaView,Image,TouchableOpacity } from 'react-native'
import React,{useState} from 'react'

const Header = () => {
     return (
    <View style={{backgroundColor:"rgb(193, 210, 226)",padding:1,display:"flex",flexDirection:"row",justifyContent:"space-between"}}>
      {/* <Image source={require('../assets/logo.png')} resizeMode="contain" style={{width:100,height:40}}/> */}
      </View>
  )
}

export default Header